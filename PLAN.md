# (총)무엇이든 물어봐 구현 계획 (PLAN)

- 대상 문서: `PRD.md`
- 작성일: 2026-08-28
- 사이클 범위: PRD에 정의된 기능 전체

## 이번 사이클 목표

**총무팀이 복지 문서를 한 번 올려두면, 직원은 아무것도 올리지 않고 접속만 해도 질문하고 답을 받을 수 있는 상태로 만든다.**

현재 구현은 "페이지를 연 사람이 직접 PDF를 올려야" 질문할 수 있고, 새로고침하면 문서가 사라진다. PRD §4는 "직원이 접속 → 질문 → **총무팀이 올려둔** 문서를 근거로 답변"이므로, 이 격차를 메우는 것이 이번 사이클의 핵심이다. 나머지 작업(정답률 측정·사내망 제한·배포)은 모두 이 구조가 선 다음에야 의미가 있다.

## 성공 기준

| # | 기준 | 확인 방법 | 근거 |
|---|------|-----------|------|
| 1 | 직원이 문서를 올리지 않고 접속만 해도 질문할 수 있다 | 시크릿 창으로 접속해 바로 질문 | PRD §4 |
| 2 | 새로고침해도 문서가 유지된다 | 새로고침 후 질문이 정상 동작 | PRD §4 |
| 3 | 질문 입력 시점부터 답변까지 1분 이내 | 실제 시간 측정 | PRD §3 |
| 4 | 테스트 질문 10~20개 기준 문서 근거 정답률 90% 이상 | 테스트 질문 세트 실행 | PRD §3 |
| 5 | 예외 상황 5가지가 PRD 문구 그대로 동작한다 | 각 상황을 재현해 문구 대조 | PRD §5 |
| 6 | `npm run lint`와 `npm run build`가 통과한다 | 두 명령 실행 | CLAUDE.md 검증 루프 |

> 사내망 제한은 필수 성공 기준에서 제외한다. PRD §7이 2026-08-28에 "직원 화면과 질문 기능은 사내망으로 제한하지 않는다"로 개정되었기 때문이다 (아래 작업 10은 선택 기능으로 남긴다).

## 작업 순서

앞 작업이 끝나면 뒤 작업이 쉬워지도록, **문서를 어디에 두느냐(1~3) → 누가 올리고 누가 읽느냐(4~8) → 품질·보안·배포(9~11) → 선택 기능(12~13)** 순으로 배열했다.

1. ✅ Supabase 프로젝트와 문서 테이블을 준비한다 (인증은 `.env`의 `SUPABASE_ACCESS_TOKEN` 사용).
2. ✅ 추출한 문서 텍스트를 Supabase에 저장하는 업로드 API를 만든다.
3. ✅ 저장된 문서의 목록과 내용을 읽어오는 조회 API를 만든다.
4. ✅ 총무팀 전용 문서 관리 화면(`/admin`)을 만들고 업로드 UI를 그쪽으로 옮긴다 (`ADMIN_PASSWORD` 비밀번호 보호 포함, `src/proxy.ts`).
5. ✅ 관리 화면에 문서 교체·삭제 기능을 넣는다 (PRD §7 문서 갱신 원칙).
6. ✅ 직원 화면(`/`)에서 업로드 UI를 걷어내고, 접속하면 저장된 문서를 자동으로 불러오게 한다.
7. ✅ `/api/chat`이 브라우저가 보낸 문서 대신 서버에 저장된 문서를 근거로 답하도록 바꾼다.
8. ✅ 저장된 문서가 하나도 없을 때 PRD 문구 그대로 화면에 표시한다. (6·7번과 함께 처리)
9. ✅ 정답률 측정용 테스트 질문 10~20개와 기대 답변을 파일로 만든다. — 규칙 문항 4건 + 경조사비.pdf 기준 실제 질문 6건, 총 10건 완료. 실측 정답률 100%(10/10), 평균 응답 1.9초.
10. ✅ (선택) 사내망 IP만 허용하는 미들웨어를 넣는다 (허용 IP 목록은 `.env`로 관리). — `src/proxy.ts` 완료. PRD §7 개정으로 필수는 아니며, `ALLOWED_IPS`가 비어 있으면 제한하지 않는다. 필요해지면 병원 공인 IP를 받아 채우면 켜진다. **Vercel 배포 환경에서만 위조 방지가 보장된다.**
11. Vercel에 배포하고 환경변수를 등록한다.
12. ✅ (선택) 자주 묻는 질문(FAQ) 기능을 만든다. — 관리 화면(`FaqManager.tsx`, `/api/faq`)에서 총무팀이 질문을 등록·삭제하면, 직원 화면(`ChatPanel.tsx`)이 대화 시작 전에 칩 버튼으로 보여주고 클릭하면 바로 그 질문을 전송한다 (최대 12개, 문항당 100자).
13. (선택) 대화 내용 저장·다시 보기 기능을 만든다.

## 현재 상태 대조

| PRD 항목 | 상태 |
|---|---|
| §5 must 1 — 문서 기반 질의응답 | 동작함. 단 문서를 브라우저가 들고 있어 §4 흐름과 불일치 |
| §5 must 2 — 신청 방법·시기 안내 | 동작함 (문서에 해당 내용이 있을 때) |
| §5 예외 — 무관한 질문 거절 | 완료 |
| §5 예외 — 문서 상충 시 최신 우선 | 완료 (프롬프트 규칙) |
| §5 예외 — 글자 못 읽는 PDF | 완료 |
| §5 예외 — OpenAI 장애 | 완료 |
| §5 예외 — 문서 없음 | 완료 — PRD §5 문구를 실제 동작(안내 문구 표시 + 입력 잠금)에 맞춰 2026-08-31 수정 |
| §4 이용 흐름 | **미완료** — 직원이 직접 업로드해야 함 |
| §3 정답률 90% 측정 | 완료 — 실측 100%(10/10) |
| §7 사내망 제한 | 완료 (선택 기능, 기본값은 비활성화 — PRD §7 개정으로 필수 아님) |
| §7 관리 화면 비밀번호 보호 | 완료 |
| §9 배포 | **미완료** |
| §5 nice — FAQ | 완료 |
| §5 nice — 대화 저장 | 미착수 |

## 결정 기록

- **문서 저장소: Supabase** (2026-08-28 확정). `.env`의 `SUPABASE_ACCESS_TOKEN`으로 인증하며, Vercel 배포 환경에서는 서버에 파일을 저장해 둘 수 없어 외부 저장소가 필요하다는 점이 근거다.

## 남은 확인 사항

- 사내망 제한(10번 작업)은 선택 기능으로 유지한다. 필요해지면 병원 전산팀에서 공인 IP를 받아 `.env`의 `ALLOWED_IPS`에 채우면 되고, 배포(11번)를 막는 필수 조건은 아니다.
- PRD §8은 기술 스택을 Next.js로 고정하고 있다. Supabase는 프레임워크 교체가 아니라 저장소 추가이므로 이 고정과 충돌하지 않는다.
