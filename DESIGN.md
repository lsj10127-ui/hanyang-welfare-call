# 한양복지콜 설계서 (DESIGN)

- 대상 문서: `PRD.md`, `PLAN.md`
- 작성일: 2026-08-28

## 한눈에 보기

이 서비스에는 **화면이 두 개**입니다.

| 화면 | 주소 | 누가 쓰나 | 무엇을 하나 |
|------|------|-----------|-------------|
| 직원 화면 | `/` | 병원 전 직원 | 복지 제도를 질문하고 답을 받는다 |
| 관리 화면 | `/admin` | 총무팀 | 복지 문서를 올리고, 바꾸고, 지운다 |

핵심은 **역할이 나뉘어 있다**는 점입니다. 총무팀이 한 번 문서를 올려두면, 직원은 아무것도 올리지 않고 접속만 해도 질문할 수 있습니다.

---

## 1. 화면 구성

### 1-1. 직원 화면 (`/`)

위에서 아래로 이렇게 보입니다.

```
┌─────────────────────────────────────────┐
│  한양복지콜                              │  ← 서비스 이름
│  복지 제도에 대해 궁금한 점을 물어보세요   │  ← 한 줄 설명
│  ✓ 안내 가능한 문서 3개                  │  ← 문서가 있을 때만 표시
├─────────────────────────────────────────┤
│                                         │
│              경조사비 얼마인가요?    ●   │  ← 내 질문 (오른쪽, 파란 말풍선)
│                                         │
│  ○  본인 결혼 시 50만원을 지급합니다.     │  ← 챗봇 답변 (왼쪽, 연한 말풍선)
│                                         │
│                              (대화가 쌓임) │
├─────────────────────────────────────────┤
│  [ 질문 입력란              ]  [보내기]  │
└─────────────────────────────────────────┘
   총무팀 문서 관리                          ← 관리 화면 링크 (작게)
```

**문서가 하나도 없을 때**는 안내 문구가 `"현재 안내 가능한 문서가 없습니다. 총무팀에 문의해 주세요."`로 바뀌고, 입력란과 보내기 버튼이 잠깁니다. 답할 근거가 없는데 질문을 받아 두면 직원이 기다리다 실망하기 때문입니다.

### 1-2. 관리 화면 (`/admin`)

```
┌─────────────────────────────────────────┐
│  ← 직원 화면으로                          │
│  복지 문서 관리                           │
│  총무팀 전용 화면입니다                    │
├─────────────────────────────────────────┤
│  ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   │
│  │        ⬆                          │   │  ← 클릭하면 파일 선택창
│  │  클릭해서 복지 문서(PDF)를 올려주세요 │   │
│  │  제도별로 나누어 올리면 정확해집니다   │   │
│  │  같은 이름이면 최신 내용으로 교체됩니다 │   │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘   │
├─────────────────────────────────────────┤
│  저장된 문서                              │
│  ✓ 문서 준비 완료 (3개)                   │
│                                         │
│  경조사지원.pdf              [ 삭제 ]     │
│  2026. 8. 28. 오후 3:44 등록              │
│  ─────────────────────────────────────  │
│  건강검진안내.pdf            [ 삭제 ]     │
│  2026. 8. 27. 오전 10:12 등록             │
└─────────────────────────────────────────┘
```

삭제 버튼을 누르면 확인 창이 한 번 뜹니다. 실수로 마지막 문서를 지우면 직원 화면이 통째로 멈추기 때문입니다.

---

## 2. 데이터 흐름

### 2-1. 총무팀이 문서를 올릴 때

```
[총무팀]                [브라우저]              [서버]           [저장소]
   │                       │                     │                 │
   │ PDF 선택 ─────────────▶│                     │                 │
   │                       │ PDF에서 글자만 뽑음   │                 │
   │                       │ (파일 자체는 안 보냄) │                 │
   │                       │ ── 이름 + 글자 ─────▶│                 │
   │                       │                     │ 내용 비었나 확인  │
   │                       │                     │ ── 저장 ────────▶│
   │                       │◀── 저장 완료 ────────│                 │
   │◀── "문서 준비 완료" ────│                     │                 │
```

**PDF에서 글자를 뽑는 일은 브라우저가 합니다.** 서버로는 뽑아낸 글자만 보냅니다. 이렇게 하면 서버에 PDF 처리 프로그램을 따로 설치하지 않아도 되고, 무거운 파일이 오가지 않습니다.

같은 이름의 파일을 다시 올리면 **새로 쌓이지 않고 교체**됩니다. 옛 버전이 남아 있으면 "지원금 30만원"과 "지원금 50만원"이 함께 답변 근거로 들어가기 때문입니다.

### 2-2. 직원이 질문할 때

```
[직원]                 [브라우저]              [서버]           [저장소]      [AI]
  │                       │                     │                 │           │
  │ 질문 입력 ────────────▶│                     │                 │           │
  │                       │ ── 질문만 ─────────▶│                 │           │
  │                       │                     │ ── 문서 요청 ───▶│           │
  │                       │                     │◀── 전체 문서 ────│           │
  │                       │                     │                 │           │
  │                       │                     │ 문서 없으면 여기서 안내하고 끝   │
  │                       │                     │                 │           │
  │                       │                     │ ── 규칙 + 문서 + 질문 ───────▶│
  │                       │                     │◀────────── 답변 ─────────────│
  │                       │◀── 답변 ────────────│                 │           │
  │◀── 말풍선에 표시 ──────│                     │                 │           │
```

**브라우저는 질문만 보냅니다. 문서는 서버가 직접 읽습니다.**

이게 중요한 이유가 있습니다. 예전 방식처럼 브라우저가 문서 내용을 함께 보내면, 누구든 요청을 조작해 `"축의금 9999만원"`이라는 가짜 문서를 끼워 넣고 원하는 답을 끌어낼 수 있습니다. 서버가 직접 읽으면 그럴 수 없습니다.

### 2-3. AI에게 보내는 것

서버는 AI에게 **세 가지**를 함께 보냅니다.

1. **지켜야 할 규칙** — 문서에 있는 것만 답할 것, 없으면 "문서에 없습니다"라고만 할 것, 금액·날짜는 문서 표현 그대로 쓸 것, 복지와 무관하면 거절할 것, 문서끼리 다르면 최신 것을 따를 것, 존댓말을 쓸 것
2. **문서 전체 내용** — 오래된 것부터 최신 순으로 (그래야 "맨 뒤가 최신" 규칙이 성립)
3. **직원의 질문**

문서를 다 합쳐 6만 자가 넘으면 뒷부분을 자르고, 잘렸다는 사실을 AI에게 알려 줍니다.

---

## 3. 기술 선택

### 3-1. 기본: Next.js (PRD §8에서 고정)

화면과 서버 코드를 한 프로젝트에서 함께 다루는 도구입니다. Part 5에서 Vercel로 배포할 예정이라 그대로 이어집니다.

### 3-2. 추가로 쓴 기술

| 기술 | 쉬운 설명 | 왜 필요한가 |
|------|-----------|-------------|
| **Supabase** | 인터넷에 있는 엑셀 시트 같은 저장 공간입니다. | 총무팀이 올린 문서가 남아 있어야 직원이 접속만으로 질문할 수 있습니다. Vercel은 서버에 파일을 계속 두지 못해서 바깥 저장소가 필요합니다. |
| **pdfjs-dist** | 브라우저가 PDF를 읽어 글자를 뽑아내는 도구입니다. | 아무 프로그램도 설치하지 않고 웹페이지 안에서 PDF를 읽기 위해서입니다. |
| **OpenAI gpt-4o-mini** | 질문에 답을 써 주는 AI입니다. 성능 대비 값이 싼 편입니다. | 문서를 근거로 자연스러운 한국어 답변을 만듭니다. |
| **Tailwind CSS** | 화면 꾸미기를 짧게 적을 수 있게 해 주는 도구입니다. | Next.js를 설치할 때 함께 들어왔고, 색을 한 곳에서 관리하기 편합니다. |

### 3-3. 저장소 구조

문서는 표 하나에 담깁니다.

| 칸 이름 | 담기는 것 | 비고 |
|---------|-----------|------|
| `id` | 문서마다 붙는 고유 번호 | 삭제할 때 쓴다 |
| `name` | 파일 이름 | **중복 불가** — 같은 이름이면 교체된다 |
| `content` | PDF에서 뽑아낸 글자 | 답변 근거 |
| `uploaded_at` | 올린 시각 | 최신 판단 기준 |

브라우저에서 저장소로 직접 접근하는 길은 막아 두었습니다(RLS). 서버만 열쇠를 가지고 있습니다.

---

## 4. 안전장치

| 무엇을 | 어떻게 |
|--------|--------|
| **API 키 보호** | `.env`에 두고 서버에서만 읽습니다. 브라우저로 나가지 않습니다. |
| **답변 조작 방지** | 문서를 브라우저에서 받지 않고 서버가 직접 읽습니다. |
| **관리 화면 보호** | `/admin`과 문서 저장·삭제는 비밀번호를 요구합니다. 브라우저가 입력창을 띄우고, 아이디는 비워둔 채 `.env`의 `ADMIN_PASSWORD` 값만 넣으면 됩니다. |
| **사내망 제한 (선택)** | `.env`의 `ALLOWED_IPS`에 병원 IP를 적으면 그 주소에서만 열립니다. 비워 두면 제한하지 않습니다. |
| **글자 없는 PDF** | 스캔 이미지처럼 글자를 못 뽑으면 저장하지 않고 다시 올려 달라고 안내합니다. |
| **문서 없음** | 질문을 받지 않고 총무팀에 문의하라고 안내합니다. |
| **AI 장애** | "잠시 후 다시 시도해 주세요"라고 안내합니다. |

**왜 관리 화면만 잠갔나**: 안내하는 복지 문서는 공개해도 되는 내용입니다. 하지만 문서를 *올리고 지우는* 일은 다른 문제입니다. 아무나 접근할 수 있으면 가짜 복지 안내를 올려 챗봇이 직원에게 잘못된 답을 하게 만들 수 있습니다. 그래서 직원 화면은 열어 두고 관리 쪽만 잠갔습니다.

**사내망 제한을 쓸 경우의 조건**: 접속자의 IP를 알려 주는 정보는 보내는 쪽이 마음대로 적을 수 있습니다. Vercel은 이 값을 자기가 확인한 값으로 덮어써서 위조를 막지만, 직접 띄운 서버 앞에 이 검사만 두면 효과가 없습니다. IP 제한을 실제로 쓰려면 Vercel에 배포해야 합니다.

---

## 5. 파일이 어디에 있나

```
my-app/
├─ src/
│  ├─ app/
│  │  ├─ page.tsx           직원 화면 (문서 수를 서버에서 읽어 온다)
│  │  ├─ ChatPanel.tsx      대화 부분 (질문 입력·말풍선)
│  │  ├─ extractPdfText.ts  PDF에서 글자 뽑기
│  │  ├─ admin/
│  │  │  ├─ page.tsx            관리 화면 (문서 목록을 서버에서 읽어 온다)
│  │  │  └─ DocumentManager.tsx 업로드·삭제 동작
│  │  └─ api/
│  │     ├─ chat/route.ts           질문을 받아 AI 답변을 돌려준다
│  │     └─ documents/route.ts      문서 저장·목록
│  │        └─ [id]/route.ts        문서 삭제
│  ├─ lib/supabase.ts       저장소 접속 (서버 전용)
│  └─ proxy.ts              사내망 IP 제한
└─ tests/accuracy/          정답률 측정 (npm run test:accuracy)
```

**화면 파일이 둘로 나뉜 이유**: 바깥쪽(`page.tsx`)은 서버에서 데이터를 미리 읽어 오고, 안쪽(`ChatPanel.tsx`, `DocumentManager.tsx`)은 사용자의 클릭과 입력을 받습니다. 이렇게 나누면 화면이 처음 뜰 때 이미 데이터가 채워져 있어 깜빡임이 없습니다.
